{{- if .Values.serviceaccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default "postgres-operator" .Values.serviceaccount.name }}
  namespace: {{ .Release.Namespace }}
{{- end }}